# Lecture Memo 사용 설명서

Windows와 Chrome에서 Lecture Memo를 처음 사용하는 분을 위한 안내서입니다.

## 시작하기 전에

- Windows 10 버전 1809 이상 또는 Windows 11을 사용합니다.
- Chrome을 설치합니다.
- Gemini API 키를 준비합니다.
- 서버를 처음 설치하는 PC라면 인터넷 연결이 필요합니다.
- Python이 없어도 됩니다. `setup-and-run-server.cmd`가 Python 3.11을 자동 설치합니다.

## 1. 프로젝트 다운로드

1. [Web-Audio-Summary GitHub 페이지](https://github.com/Lear-an/Web-Audio-Summary)에 접속합니다.
2. `Code` 버튼을 누르고 `Download ZIP`을 선택합니다.
3. ZIP 파일의 압축을 원하는 위치에 풉니다.
4. 압축을 푼 폴더 안에 `extension` 폴더와 `setup-and-run-server.cmd` 파일이 함께 있는지 확인합니다.

## 2. Chrome 확장 프로그램 설치

1. Chrome 주소창에 `chrome://extensions`를 입력합니다.
2. 오른쪽 위의 `개발자 모드`를 켭니다.
3. `압축해제된 확장 프로그램을 로드합니다`를 누릅니다.
4. 방금 압축을 푼 프로젝트 폴더 안의 `extension` 폴더를 선택합니다.
5. 확장 프로그램 목록에 `Lecture Memo`가 표시되는지 확인합니다.

## 3. 로컬 서버 시작

1. 프로젝트 폴더의 `setup-and-run-server.cmd`를 더블클릭합니다.
2. 검은색 서버 창이 열리면 닫지 말고 그대로 둡니다.
3. 처음 실행하는 PC에 Python이 없으면 `winget`을 이용해 Python 3.11을 자동 설치합니다.
4. 이어서 가상환경을 만들고 FastAPI, Gemini SDK 등 필요한 패키지를 자동 설치합니다.
5. 서버 창에서 아래와 같은 줄을 찾습니다.

   ```text
   Lecture Memo local access token: 여기에 표시된 문자열
   ```

6. `:` 뒤에 표시된 토큰 문자열만 복사합니다.

서버 주소는 기본적으로 다음과 같습니다.

```text
http://127.0.0.1:8050
```

PowerShell에서 실행할 때는 프로젝트 폴더로 이동한 뒤 다음 명령을 사용합니다.

```powershell
.\setup-and-run-server.cmd
```

`winget`이 없는 PC에서는 Python 자동 설치를 진행할 수 없습니다. 이 경우 PowerShell 또는 명령 프롬프트에서 아래 명령으로 먼저 확인합니다.

```powershell
winget --version
```

## 4. Gemini 설정

최초 설치 시 `server\.env`가 자동 생성되며, 실제 Gemini API를 사용하도록 `MOCK_GEMINI=false`로 설정됩니다.

API 호출 없이 UI 흐름만 테스트하려면:

1. 서버 창을 종료합니다.
2. `server\.env` 파일을 엽니다.
3. 다음 값을 변경합니다.

   ```dotenv
   MOCK_GEMINI=true
   ```

4. 파일을 저장합니다.
5. `setup-and-run-server.cmd`를 다시 실행합니다.

실제 Gemini를 사용할 때는 `MOCK_GEMINI=false`를 유지하고, 캡처 시작 시 사이드패널에 Gemini API 키를 입력합니다.

Gemini API 키는 `.env`에 저장하지 않고, 캡처 시작 시 Chrome 사이드패널에 입력합니다.

## 5. 캡처 시작

1. Chrome에서 음성을 캡처할 강의 영상 탭을 엽니다.
2. 영상이 재생되는 탭을 현재 선택합니다.
3. Chrome 오른쪽 위의 확장 프로그램 아이콘을 누릅니다.
4. `Lecture Memo`를 선택해 사이드패널을 엽니다.
5. 사이드패널에 다음 정보를 입력합니다.

   - 서버 주소: `http://127.0.0.1:8050`
   - 액세스 토큰: 서버 창에서 복사한 토큰
   - Gemini API 키: 발급받은 Gemini API 키

6. `캡처 시작`을 누릅니다.
7. 강의 영상의 재생 버튼을 누릅니다.

캡처 중에는 서버의 검은색 창과 Chrome 강의 탭을 닫지 마세요.

## 6. 캡처 종료 및 내보내기

1. 강의가 끝나면 사이드패널에서 `캡처 종료`를 누릅니다.
2. 최종 요약과 남은 자막 처리가 끝날 때까지 기다립니다. 대기 청크가 있으면 최대 5~15분 걸릴 수 있으며, 이때 브라우저와 서버 창을 닫지 마세요.
3. 필요한 형식으로 결과를 내보냅니다.

지원 형식:

- Markdown (`.md`)
- Text (`.txt`)
- SubRip 자막 (`.srt`)
- WebVTT 자막 (`.vtt`)

## 자주 발생하는 문제

### `'setup-and-run-server.cmd' is not recognized`가 표시되는 경우

PowerShell에서는 현재 폴더의 파일을 실행할 때 `./` 또는 `.`를 붙여야 합니다.

```powershell
.\setup-and-run-server.cmd
```

또는 먼저 프로젝트 폴더로 이동했는지 확인합니다.

### `winget`을 찾을 수 없는 경우

`App Installer`가 설치되어 있지 않거나 Windows 정책으로 비활성화된 경우입니다. Microsoft Store에서 `App Installer`를 설치한 뒤 다시 시도합니다.

### `422 Unprocessable Entity`가 표시되는 경우

- 액세스 토큰을 서버 창의 값과 정확히 같게 입력했는지 확인합니다.
- Gemini API 키를 입력했는지 확인합니다.
- 서버 주소가 `http://127.0.0.1:8050`인지 확인합니다.
- 확장 프로그램을 새로 로드했다면 강의 탭을 새로고침하고 사이드패널을 다시 엽니다.

### `할당량 소진`이 표시되는 경우

Gemini API의 요청 한도를 초과한 경우입니다. 프로그램은 실패한 청크와 대기 큐를 메모리에 보존하고 녹음을 일시정지합니다.

1. Gemini 프로젝트의 사용량과 할당량을 확인합니다.
2. 사이드패널의 활성화된 `Gemini API 키` 입력란에 계속 사용할 다른 키를 입력합니다.
3. `새 키로 계속`을 누릅니다.

새 키는 현재 서버 세션에만 적용되고 입력란에서 즉시 지워집니다. 보존된 청크부터 처리한 후 녹음이 재개됩니다. 같은 프로젝트에서 발급된 키는 할당량을 공유하므로 이미 소진된 프로젝트의 다른 키로는 재개되지 않을 수 있습니다.

새 키를 입력하지 않고 캡처를 종료하면 보존된 청크는 처리할 수 없으며, 사이드패널에 처리되지 않은 청크 개수가 표시됩니다.

### `Gemini 모델 혼잡`이 표시되는 경우

Gemini가 일시적으로 503 응답을 반환한 경우입니다. 실패한 오디오 청크는 버리지 않고 메모리 큐 맨 앞에 보존되며 15초, 30초, 이후 최대 45초 간격으로 자동 재시도합니다. 큐가 24MB를 넘으면 경고하고 32MB에 도달하면 새 녹음을 일시정지하며, 12MB 이하가 되면 자동 재개합니다.

캡처 종료 시에도 남은 청크를 5~15분 동안 처리합니다. 제한시간을 넘긴 청크가 있으면 사이드패널에 누락 개수가 표시됩니다.

### `Extension context invalidated`가 표시되는 경우

Chrome 확장 프로그램을 새로고침한 뒤 기존 강의 탭이 이전 확장 프로그램 연결을 계속 사용하는 상황입니다.

1. 강의 탭을 새로고침합니다.
2. 사이드패널을 닫았다가 다시 엽니다.
3. 필요하면 `Lecture Memo` 확장 프로그램을 다시 로드합니다.

### `No module named 'fastapi'`가 표시되는 경우

전역 Python으로 서버를 직접 실행했거나 패키지 설치가 완료되지 않은 경우입니다. 서버 폴더에서 직접 실행하지 말고 프로젝트 루트의 `setup-and-run-server.cmd`를 다시 실행합니다.

## 종료 방법

캡처를 종료한 뒤 서버 창에서 `Ctrl+C`를 누르면 서버를 종료할 수 있습니다.

API 키는 캡처 시작 과정에서만 사용되며 확장 프로그램 저장소나 `server\.env`에 저장되지 않습니다.
