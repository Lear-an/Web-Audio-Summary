# Lecture Memo V5 사용 설명서

이 문서는 관리자가 Render·Atlas를 준비하고, 최대 7명의 사용자가 Chrome 확장 프로그램으로 강의를 전사하는 방법을 설명합니다.

## 1. 사용자가 준비할 정보

관리자는 사용자마다 다음 두 값을 개별 전달합니다.

- 사용자 ID: 예) `user-001`
- 접속 코드: 사용자별로 다른 충분히 긴 무작위 문자열

사용자는 관리자에게 받은 사용자 ID와 접속 코드만 준비합니다. OpenAI API 키는 운영자가 Render에 설정합니다.

## 2. 확장 프로그램 설치

1. [Web-Audio-Summary GitHub 페이지](https://github.com/Lear-an/Web-Audio-Summary)에서 `Code > Download ZIP`을 선택합니다.
2. ZIP의 압축을 풉니다.
3. Chrome 주소창에 `chrome://extensions`를 입력합니다.
4. 오른쪽 위의 `개발자 모드`를 켭니다.
5. `압축해제된 확장 프로그램을 로드합니다`를 누릅니다.
6. 압축을 푼 폴더 안의 `extension` 폴더를 선택합니다.

운영자가 V5 배포 설정을 끝낸 ZIP을 제공한 경우 로컬 서버를 실행할 필요가 없습니다. 확장 프로그램에는 Render 주소가 미리 고정되어 있습니다.

## 3. 캡처 시작

1. Chrome에서 캡처할 강의 영상 탭을 엽니다.
2. 그 탭을 선택한 상태에서 Chrome 툴바의 `Lecture Memo`를 눌러 사이드패널을 엽니다.
3. 사용자 ID와 접속 코드를 입력합니다.
4. `캡처 시작`을 누르고 영상을 재생합니다.

기본 청크 창은 60초이며 앞 청크와 2초가 겹칩니다. 자막은 청크 처리가 끝난 뒤 표시됩니다. 다른 탭에서 작업해도 선택한 캡처 탭 하나의 오디오만 수집합니다. Chrome 탭 자체를 음소거하면 캡처 오디오가 사라질 수 있으므로 탭 볼륨은 유지하고, 소리를 듣고 싶지 않다면 Windows 출력 볼륨을 낮추는 방식을 권장합니다.

## 4. 캡처 종료와 결과 저장

1. 강의가 끝나면 `캡처 종료`를 누릅니다.
2. 남은 청크 처리와 최종 요약이 끝날 때까지 기다립니다.
3. 필요하면 MD, TXT, SRT, VTT로 내려받습니다.

중간 요약은 생성하지 않습니다. 모든 청크가 정상 처리된 경우에만 종료 시 최종 요약을 한 번 생성하고 Atlas에 최종 문서를 저장합니다. 처리되지 않은 청크가 있으면 최종 요약 없이 `incomplete` 초안으로 자동 보관됩니다.

## 5. 오류와 복구

### 할당량 소진

OpenAI quota `429`가 발생하면 자동 재시도를 멈추고 실패 청크를 IndexedDB에 보존합니다.

1. 관리자에게 OpenAI 결제·사용 한도 확인을 요청합니다.
2. 운영자가 조치를 마치면 `처리 다시 시도`를 누릅니다.
3. 보존 청크부터 처리한 뒤 캡처가 재개됩니다.

운영자 키는 사용자 화면에 노출되지 않으므로 키 교체가 필요하면 Render Secret에서 변경하고 재배포합니다.

### OpenAI 모델 혼잡

`503`이면 원본 청크를 버리지 않고 15초, 30초, 45초 간격으로 재시도합니다. 계속 실패하면 큐에 남긴 상태로 기다립니다.

### Render 서버 첫 연결 지연

무료 Render 서비스가 절전 상태였다면 첫 연결이 늦을 수 있습니다. 확장 프로그램은 `/health/ready`를 최대 세 번 확인합니다. 잠시 후에도 실패하면 운영자에게 Render 상태를 문의하세요.

### 브라우저 또는 Render 재시작

ACK되지 않은 오디오는 브라우저 IndexedDB에 남고, 처리된 자막 초안은 Atlas에 남습니다. 같은 강의 URL에서 다시 캡처를 시작하면 기존 세션을 복구합니다.

캡처 중 Render가 재시작되어 `서버 세션을 먼저 복구해 주세요`라는 메시지가 표시되면 `보존 청크 폐기`를 누르지 마세요. `서버 세션 복구`를 누르면 서버 세션을 재생성한 뒤 보존 청크를 순서대로 다시 전송합니다. 복구 중인 캡처 탭은 닫지 않아야 합니다.

### 저장 공간 부족 또는 처리 불가능한 청크

IndexedDB 임시 보관 한도는 128 MiB이며, 미처리 청크는 72시간 보존합니다. 한도 도달, 파일 크기 초과, 지원하지 않는 형식 등의 경우 캡처가 멈추고 `사용자 조치 필요`가 표시됩니다. `보존 오디오 내보내기`로 청크별 WebM 파일을 먼저 내려받을 수 있습니다. Chrome이 여러 파일 다운로드 허용 여부를 물으면 허용해야 전체 청크를 받을 수 있습니다. 확인 후 `보존 청크 폐기`를 누를 수 있으며 폐기는 되돌릴 수 없습니다.

### `Extension context invalidated`

확장 프로그램을 다시 로드한 뒤 기존 강의 탭이 이전 실행 컨텍스트를 참조하는 경우입니다. 강의 탭을 새로고침하고 사이드패널을 다시 여세요.

## 6. 로컬 개발 모드

운영 Render 대신 로컬에서 시험할 때만 프로젝트 루트의 `setup-and-run-server.cmd`를 사용합니다.

```powershell
.\setup-and-run-server.cmd
```

스크립트는 Python 3.11 이상을 확인하고, 없으면 `winget`으로 Python 3.11을 설치한 뒤 가상환경과 의존성을 준비합니다. 서버 창에 표시된 로컬 토큰을 접속 코드 칸에 입력합니다. 기본 주소는 `http://127.0.0.1:8050`입니다.

PowerShell에서 파일명만 입력하면 현재 폴더의 스크립트를 찾지 않으므로 반드시 `.\`를 붙입니다.

## 7. 운영자 Render·Atlas 설정

Render에서는 `Web Service`를 선택합니다. Static Site, Cron Job, Background Worker가 아닙니다.

필수 설정:

```text
Build Command: pip install -r server/requirements.txt
Start Command: uvicorn server.app:app --host 0.0.0.0 --port $PORT --workers 1
Health Check Path: /health/live
Plan: Free
Auto-Deploy: Off
```

필수 비밀 환경변수:

```dotenv
APP_AUTH_MODE=multi_user
APP_USER_1_ID=user-001
APP_USER_1_TOKEN_SHA256=<사용자 1 접속 코드의 SHA-256>
MONGODB_URI=mongodb+srv://...
MONGODB_REQUIRED=true
ALLOWED_EXTENSION_ORIGINS=chrome-extension://<고정 확장 ID>
OPENAI_API_KEY=<운영자 OpenAI API 키>
SAFETY_IDENTIFIER_SECRET=<충분히 긴 무작위 비밀값>
```

사용자는 최대 10명까지 `APP_USER_1_*`부터 `APP_USER_10_*`로 등록할 수 있고 동시 활성 사용자는 기본 5명입니다. 실제 접속 코드는 관리자의 비밀번호 관리 도구에만 보관하세요.

운영 배포 전에는 다음 값을 확인해야 합니다.

- 실제 Render HTTPS URL은 `extension/config.js`와 `extension/manifest.json`에 이미 `https://web-audio-summary.onrender.com`으로 반영되어 있습니다.
- Chrome Developer Dashboard 공개 키: `extension/manifest.json`의 `key`

GitHub Actions CI가 통과한 것을 확인한 뒤 Render Dashboard에서 `Manual Deploy > Deploy latest commit`을 선택합니다.

## 개인정보 안내

오디오 청크는 운영자의 Render 서버를 거쳐 OpenAI API로 전송됩니다. 처리된 자막 초안과 최종 자막·요약은 운영자의 MongoDB Atlas에 사용자 ID와 함께 저장됩니다. 원본 오디오는 Atlas나 Render 디스크에 저장하지 않으며, ACK 전 청크만 사용자 브라우저 IndexedDB에 임시 보관합니다.
